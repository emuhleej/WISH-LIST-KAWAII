{
  "name": "curated-wish-list",
  "version": "1.0.0",
  "description": "A single-page curated wish list app.",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "coverage": "vitest run --coverage",
    "e2e": "playwright test",
    "test:rules": "firebase emulators:exec --only firestore --project demo-wishlist \"vitest run --config vitest.rules.config.js\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.1",
    "@playwright/test": "^1.49.1",
    "firebase": "^12.16.0",
    "firebase-tools": "^15.24.0",
    "vitest": "^2.1.8"
  }
}
